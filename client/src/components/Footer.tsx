import { Box, Typography } from "@mui/material";

const Footer = () => {
    return (
        <Box mt={30} width="100%" height={150} sx={{ background: "#2F2F2F" }}>
            <Typography>Hello World!</Typography>

        </Box>
    );
};

export default Footer;
